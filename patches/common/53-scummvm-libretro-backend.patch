diff --git a/packages/lakka/libretro_cores/libretro-scummvm-backend/package.mk b/packages/lakka/libretro_cores/libretro-scummvm-backend/package.mk
new file mode 100644
index 0000000000..52dd78377f
--- /dev/null
+++ b/packages/lakka/libretro_cores/libretro-scummvm-backend/package.mk
@@ -0,0 +1,7 @@
+PKG_NAME="libretro-scummvm-backend"
+PKG_VERSION="96a63485f3be7888f15cd66215d1e4eaf9d3070b"
+PKG_SITE="https://github.com/diablodiab/libretro-scummvm-backend"
+PKG_URL="${PKG_SITE}.git"
+PKG_DEPENDS_TARGET=""
+PKG_LONGDESC="Libretro backend for ScummVM which can be added to backends/platform folder of ScummVM base to build a Libretro core of ScummVM"
+PKG_TOOLCHAIN="manual"
diff --git a/packages/lakka/libretro_cores/libretro-scummvm-backend/patches/01.add_overlayInGUI.patch b/packages/lakka/libretro_cores/libretro-scummvm-backend/patches/01.add_overlayInGUI.patch
new file mode 100644
index 0000000000..fe9bd1b23c
--- /dev/null
+++ b/packages/lakka/libretro_cores/libretro-scummvm-backend/patches/01.add_overlayInGUI.patch
@@ -0,0 +1,49 @@
+diff --git a/libretro_os.cpp b/libretro_os.cpp
+index 0c7b4d7..de5fc31 100755
+--- a/libretro_os.cpp
++++ b/libretro_os.cpp
+@@ -351,6 +351,7 @@ class OSystem_RETRO : public EventsBaseBackend, public PaletteManager {
+
+       Graphics::Surface _overlay;
+       bool _overlayVisible;
++      bool _overlayInGUI;
+
+       Graphics::Surface _mouseImage;
+       RetroPalette _mousePalette;
+@@ -535,7 +536,7 @@ class OSystem_RETRO : public EventsBaseBackend, public PaletteManager {
+
+       virtual void updateScreen()
+       {
+-         const Graphics::Surface& srcSurface = (_overlayVisible) ? _overlay : _gameScreen;
++         const Graphics::Surface& srcSurface = (_overlayInGUI) ? _overlay : _gameScreen;
+          if(srcSurface.w && srcSurface.h)
+          {
+             switch(srcSurface.format.bytesPerPixel)
+@@ -590,14 +591,16 @@ class OSystem_RETRO : public EventsBaseBackend, public PaletteManager {
+          // TODO
+       }
+
+-      virtual void showOverlay()
++      virtual void showOverlay(bool inGUI)
+       {
+-         _overlayVisible = true;
++        _overlayVisible = true;
++        _overlayInGUI = inGUI;
+       }
+
+       virtual void hideOverlay()
+       {
+          _overlayVisible = false;
++         _overlayInGUI = false;
+       }
+
+       virtual void clearOverlay()
+@@ -840,7 +843,7 @@ class OSystem_RETRO : public EventsBaseBackend, public PaletteManager {
+
+       const Graphics::Surface& getScreen()
+       {
+-         const Graphics::Surface& srcSurface = (_overlayVisible) ? _overlay : _gameScreen;
++         const Graphics::Surface& srcSurface = (_overlayInGUI) ? _overlay : _gameScreen;
+
+          if(srcSurface.w != _screen.w || srcSurface.h != _screen.h)
+          {
diff --git a/packages/lakka/libretro_cores/libretro-scummvm-backend/patches/02.fix_localized_scumm_quitprompt.patch b/packages/lakka/libretro_cores/libretro-scummvm-backend/patches/02.fix_localized_scumm_quitprompt.patch
new file mode 100644
index 0000000000..4148fc3888
--- /dev/null
+++ b/packages/lakka/libretro_cores/libretro-scummvm-backend/patches/02.fix_localized_scumm_quitprompt.patch
@@ -0,0 +1,39 @@
+diff --git a/libretro_os.cpp b/libretro_os.cpp
+index d789cde..56ea91f 100755
+--- a/libretro_os.cpp
++++ b/libretro_os.cpp
+@@ -32,6 +32,7 @@
+ #include "surface.libretro.h"
+ #include "backends/base-backend.h"
+ #include "common/events.h"
++#include "common/config-manager.h"
+ #include "audio/mixer_intern.h"
+
+ #if defined(_WIN32)
+@@ -429,6 +430,14 @@ class OSystem_RETRO : public EventsBaseBackend, public PaletteManager {
+          EventsBaseBackend::initBackend();
+       }
+
++      virtual void engineInit(){
++         Common::String engineId = ConfMan.get("engineid");
++         if ( engineId.equalsIgnoreCase("scumm") && ConfMan.getBool("original_gui") ){
++            ConfMan.setBool("original_gui",false);
++            log_cb(RETRO_LOG_INFO, "\"original_gui\" setting forced to false\n");
++         }
++      }
++
+       virtual bool hasFeature(Feature f)
+       {
+          return (f == OSystem::kFeatureCursorPalette);
+@@ -1317,11 +1326,6 @@ class OSystem_RETRO : public EventsBaseBackend, public PaletteManager {
+          Common::Event ev;
+          ev.type = Common::EVENT_QUIT;
+          dynamic_cast<OSystem_RETRO *>(g_system)->getEventManager()->pushEvent(ev);
+-
+-         // Some engines might ask for a (Y/N) confirmation when quitting
+-         // Send 'y' key to suppress these confirmation prompts
+-         processKeyEvent(true, 121, 121, 0);
+-         processKeyEvent(false, 121, 121 ,0);
+       }
+ };
+
diff --git a/packages/lakka/libretro_cores/scummvm/patches/scummvm-01-add-oga-platform.patch b/packages/lakka/libretro_cores/libretro-scummvm-backend/patches/scummvm-01-add-oga-platform.patch
similarity index 85%
rename from packages/lakka/libretro_cores/scummvm/patches/scummvm-01-add-oga-platform.patch
rename to packages/lakka/libretro_cores/libretro-scummvm-backend/patches/scummvm-01-add-oga-platform.patch
index b09e47f9d2..6cae1809ad 100644
--- a/packages/lakka/libretro_cores/scummvm/patches/scummvm-01-add-oga-platform.patch
+++ b/packages/lakka/libretro_cores/libretro-scummvm-backend/patches/scummvm-01-add-oga-platform.patch
@@ -1,5 +1,5 @@
---- a/backends/platform/libretro/build/Makefile
-+++ b/backends/platform/libretro/build/Makefile
+--- a/build/Makefile
++++ b/build/Makefile
 @@ -226,6 +226,18 @@
  endif
     DEFINES += -DARM
diff --git a/packages/lakka/libretro_cores/scummvm/package.mk b/packages/lakka/libretro_cores/scummvm/package.mk
index 98669cbf2a..2f0c3d9efa 100644
--- a/packages/lakka/libretro_cores/scummvm/package.mk
+++ b/packages/lakka/libretro_cores/scummvm/package.mk
@@ -1,24 +1,36 @@
 PKG_NAME="scummvm"
-PKG_VERSION="2fb2e4c551c9c1510c56f6e890ee0300b7b3fca3"
+PKG_VERSION="c8da63062af6b24e350a584d00544d65ee3ff161"
 PKG_LICENSE="GPLv2"
-PKG_SITE="https://github.com/libretro/scummvm"
-PKG_URL="${PKG_SITE}.git"
-PKG_DEPENDS_TARGET="toolchain"
-PKG_LONGDESC="ScummVM with libretro backend."
+PKG_SITE="https://github.com/scummvm/scummvm"
+PKG_URL="$PKG_SITE/archive/$PKG_VERSION.tar.gz"
+PKG_DEPENDS_TARGET="toolchain libretro-scummvm-backend"
+PKG_SHORTDESC="Script Creation Utility for Maniac Mansion Virtual Machine"
+PKG_LONGDESC="ScummVM is a program which allows you to run certain classic graphical point-and-click adventure games, provided you already have their data files."
 PKG_TOOLCHAIN="make"
 
-PKG_MAKE_OPTS_TARGET="-C ../backends/platform/libretro/build/"
+pre_configure_target() {
+  rm -rf ../.$TARGET_NAME
+  [ -d ${PKG_BUILD}/backends/platform/libretro ] && rm -rf ${PKG_BUILD}/backends/platform/libretro
+  cp -vr ${PKG_BUILD}/../libretro-scummvm-backend-* ${PKG_BUILD}/backends/platform/libretro
+}
 
 pre_make_target() {
   CXXFLAGS+=" -DHAVE_POSIX_MEMALIGN=1"
+  CFLAGS+=" -fPIC"
   if [ "${DEVICE}" = "OdroidGoAdvance" ]; then
     PKG_MAKE_OPTS_TARGET+=" platform=oga_a35_neon_hardfloat"
   fi
 }
 
+make_target() {
+  make -C ${PKG_BUILD}/backends/platform/libretro/build
+  cd ${PKG_BUILD}/backends/platform/libretro/aux-data
+  ./bundle_aux_data.bash
+}
+
 makeinstall_target() {
   mkdir -p ${INSTALL}/usr/lib/libretro
-    cp -v ../backends/platform/libretro/build/scummvm_libretro.so ${INSTALL}/usr/lib/libretro/
+    cp -v ${PKG_BUILD}/backends/platform/libretro/build/scummvm_libretro.so ${INSTALL}/usr/lib/libretro/
 
   # unpack files to retroarch-system folder and create basic ini file
   if [ -f ${PKG_BUILD}/backends/platform/libretro/aux-data/scummvm.zip ]; then
